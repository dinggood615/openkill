# OpenKill 2026-1149

## NaiveProxy page workflow

- Added an in-page modal for adding, importing and managing NaiveProxy nodes
  from Compatibility & Auxiliary.
- Kept the existing server editor as the single UCI and credential owner;
  the modal preserves the selected YAML file and stable node identities.
- Moved the NaiveProxy bridge mode field into the NaiveProxy card and added
  responsive modal styling for desktop and narrow screens.

Validation: NaiveProxy integration, UI contract and preview tests, POSIX
checks, `scripts/local-gate.sh`, exact Development CI and the prior release
workflow. Device and remote endpoint validation remain pending.
