# OpenKill 2026-1140

## NaiveProxy component status refresh

- Refresh the optional component state from the configured executable on every
  status request and after a successful installation.
- Distinguish an installed executable with no enabled nodes from a missing
  component; stale pre-install state can no longer keep the UI at “组件未安装”.
- Preserve node configuration and keep local SOCKS5 readiness and remote
  authentication as separate, unverified stages.
- Retain the 2026-1139 BusyBox `.tar.xz` and `hexdump` compatibility fixes.
