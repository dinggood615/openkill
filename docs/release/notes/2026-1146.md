# OpenKill 2026-1146

## NaiveProxy 一键安装与节点桥接

- 兼容与辅助页的一键安装按钮继续使用官方资产匹配、摘要绑定和后台任务，并明确显示安装阶段。
- NaiveProxy 节点新增独立入口和紧凑分享链接导入框，复用现有结构化解析器及稳定节点身份。
- 兼容页提供不含凭据的本地 SOCKS5 YAML 预览和复制按钮，内容与 Mihomo 生成的回环桥接一致，UDP 保持关闭。
- 未启用 NaiveProxy 节点时不启动辅助进程；安装、本地入口和远端验证仍分别显示。

上一版本 `v2026-1145-ipk` 保留，可用于回滚。
