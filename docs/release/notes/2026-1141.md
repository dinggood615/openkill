# OpenKill 2026-1141

本版本调整设置页面的导航与卡片布局：

- “兼容设置”更名为“兼容与辅助”，移动到“网络与分流”和“规则与订阅”之间。
- 保留旧兼容标签识别和 NaiveProxy 旧书签跳转。
- 移除“隐藏高级设置”入口，“系统维护”默认显示并保持展开。
- “DNS 与本地解析”与“IPv6 与 TUN”左右并列。
- “LAN/WAN 访问控制”与“流量转发”左右并列，并统一卡片间距和响应式行为。

本版本只改变设置页面导航、展示结构和样式，不改变 DNS、IPv6、TUN、访问控制或流量转发策略。旧版本保留，可按发布说明回滚。
