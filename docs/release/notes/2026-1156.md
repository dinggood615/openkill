# OpenKill 2026-1156

## NaiveProxy 逐节点健康检查

- 兼容与辅助页面按稳定节点 ID 展示组件、辅助进程、回环入口、最终 Mihomo 节点、策略组和最近探测状态。
- 自动接入模式对指定 Mihomo SOCKS5 节点执行固定 HTTPS 延迟探测；自管 YAML 模式只验证对应回环 SOCKS5 辅助链路，并明确区分两者。
- 提供单节点和全部启用节点测试、可选有界周期检测、结果过期提示及有限任务去重；远端故障不会静默切换为 DIRECT 或重启无关实例。
- 探测只使用固定的 HTTPS 目标、有限超时和禁止重定向，状态缓存不保存凭据、分享链接或响应正文。

## 验证边界

- 本地 NaiveProxy 健康夹具、集成/UI 合同、POSIX 语法、CSS 裁剪、local-gate、精确 Development CI 和 RC 包审计已通过。
- 本版未改变 DNS、路由、防火墙、透明接管或 UDP 语义；设备、真实 Naive 服务端、远端认证、UDP、IPv6、流媒体和真实浏览器多视口仍需单独验证。
