# OpenKill 2026-1152

- 修复 NaiveProxy 兼容卡片的“添加节点”和“导入分享链接”入口，直接创建待编辑的 NaiveProxy 节点草稿并打开现有节点编辑器，不再绕到通用服务器列表。
- 修复透明接管模式下 NaiveProxy 辅助进程出站被再次接管造成的套接字耗尽；辅助实例继续以 root 读取受保护配置，但使用 OpenKill 已约定的 `nogroup` 自有流量豁免。
- 设备验证清理了此前测试机中的私人 NaiveProxy 节点配置；仓库与发布包不包含个人节点、凭据或分享链接。
- NaiveProxy 远端认证、UDP、IPv6 和流媒体业务仍需用户自己的服务端与客户端单独验证。
