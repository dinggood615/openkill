# OpenKill 2026-1145

本版本优化运行状态页的布局和状态摘要：

- 顶部“运行状态”“控制面板”“混合代理”按 2:1:1 响应式比例排列，窄屏自动转为通栏或单列。
- 五张 DNS、广告、OpenVPN、RustDesk、NaiveProxy 状态卡改为短摘要，保留生成、加载、应用和连接验证的区别；完整证据可通过状态详情查看。
- 运行中、未启动和启动失败时，左右内容列共享伸展边界；配置文件与资源指标自然对齐，不添加虚假指标或固定占位高度。
- 保留现有按钮、配置文件操作、状态接口和网络策略；浏览器运行时不可用的环境不宣称完成渲染验证。

上一版本 `v2026-1144-ipk` 保留，可用于回滚。
