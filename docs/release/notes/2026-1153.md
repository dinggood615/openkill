# OpenKill 2026-1153

- 修复 NaiveProxy“一键安装”在已有官方 URL 与 SHA256 时仍强制刷新 GitHub 元数据、导致检测失败后不发起安装的问题。
- 元数据请求失败现在会返回明确原因；完整且成对的 URL/SHA256 会直接创建后台安装任务，仍由安装器执行 HTTPS、摘要、归档、架构和加载器校验。
- 设备验证确认当前测试设备的组件已可执行；设备直连 GitHub 时仍受 Fake-IP DNS/TLS 引导限制，下载失败会保留原组件并显示具体下载阶段，未将该环境限制伪装成安装成功。
- 不包含节点、订阅、密码或其他私人配置；NaiveProxy 远端连接、UDP、IPv6 和流媒体业务仍需用户环境单独验证。
