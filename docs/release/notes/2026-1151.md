# OpenKill 2026-1151

- 修复 NaiveProxy 节点“添加节点”“导入分享链接”和“管理节点”入口在 URL 编码配置路径下触发 LuCI `bad argument #2 to format` 的问题。
- 编辑链接现在只替换显式 `%s` 占位符，保留 `file=%2F...` 等已编码查询参数。
- 增加回归契约，覆盖编码配置路径和 NaiveProxy 编辑入口。
- 不改变 DNS、路由、防火墙、节点凭据或桥接策略；远端 NaiveProxy 认证仍需用户自己的服务端验证。